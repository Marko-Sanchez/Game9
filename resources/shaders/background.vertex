#version 330 core

layout (location = 0) in vec4 a_position;
layout (location = 0) in vec2 a_uvs;

out vec2 texCords;

uniform mat4 u_model;
uniform mat4 u_projection;

void main()
{
    texCords    = a_uvs;
    gl_Position = u_projection * u_model * vec4(a_position.x, a_position.y, 0.0, 1.0);
}
